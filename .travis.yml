language: node_js
node_js:
  - '14'

branches:
  only:
    - master

cache:
  directories:
    - node_modules

install:
  - npm install
  - npm install --save-dev @babel/plugin-proposal-private-property-in-object

script:
  - npm run build
  - npm test

before_deploy:
  - cd frontend
  - npm run build

deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master

notifications:
  email: false